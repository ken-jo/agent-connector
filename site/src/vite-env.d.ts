/// <reference types="vite/client" />

/** Build-time HEAD commit date (`YYYY-MM-DD`), injected by Vite `define`. */
declare const __DOCS_BUILD_DATE__: string;

/** agent-connector package name, injected from the root package.json. */
declare const __AGENT_CONNECTOR_PACKAGE_NAME__: string;

/** agent-connector CLI bin name, injected from the root package.json. */
declare const __AGENT_CONNECTOR_BIN_NAME__: string;
