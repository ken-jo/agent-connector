# Project Memory

Last updated: 2026-09-02

## Current Product Direction

agent-connector should focus on MCP implementers: developers who already have, or are about to build, an MCP server and need to ship it across agent CLIs, IDE extensions, and apps without maintaining one integration per host.

The public site should not read like a generic agent-user tool. It should explain MCP terms, implementation steps, packaging choices, host coverage confidence, and why the framework belongs in the developer's own branded MCP package.

## Improvements To Finish First

1. Keep marketing coverage focused on recognizable or production-relevant hosts. Public coverage surfaces should hide open-source hosts below 1,000 GitHub stars, while preserving the full adapter registry in code and technical references.
2. Keep the root-level `/docs/guides` track focused on beginner education. The agent-connector beginner guide should teach protocol architecture, terms, surfaces, tool contracts, server lifecycle, tool-call flow, transports, verification, debugging, and safety while explaining how agent-connector maps those concepts into host CLIs; adjacent guide pages should explain host hooks, HUD/statusline, actions, and special surfaces in beginner terms.
3. Add a blog surface for AI and product writing. Until editorial direction is reviewed, publish only a single BUILDING test post that verifies routing, RSS, and image rendering instead of exposing draft-like articles.
4. Keep release and verification hygiene visible: repository version, npm latest, GitHub release, CI, and live host verification should not drift silently.
5. Keep low-memory development ergonomics documented and scriptable; the README warns about single-fork testing, so package scripts should expose that path directly.

## Next Development Direction

- Make the scaffold/wizard path the primary onboarding path for MCP developers.
- Expand `install <source>` beyond GitHub source specs toward npm/registry/archive intake.
- Increase live verification depth by tier, not by raw platform count: full E2E where possible, live-accept where auth blocks model turns, install/doctor placement for GUI-only hosts.
- Continue reducing duplicated support claims by generating public docs from adapter capabilities and keeping human-authored provenance separate.
- Keep statusline/action customization conservative and capability-driven. SDK knobs should expose only what adapters can honestly map today; hosts with preset-only or undocumented UI should skip-warn instead of being forced into a fake `render(ctx)` or action affordance model.
- Treat the open Agent Plugins spec (https://agent-plugins.org, v1.0.0 published 2026-08-06; Vercel-led, co-maintained with AWS, Cursor, GitHub, Microsoft, OpenAI) as the primary shared-distribution target going forward. Next steps after the `agent-plugin` emitter: (1) an `install <source>` intake that accepts an Agent Plugins directory/git URL and maps it onto the connector model; (2) a `doctor`/`audit` validation pass against the published `plugin.schema.json` + `mcp.schema.json`; (3) track the in-repo 1.1.0 draft and any client-namespace conventions still undocumented — `com.github.copilot` and `com.openai` are confirmed and implemented, Kiro documents `dev.kiro/` (steering only) and OpenClaw `ai.openclaw` (activation only), while Cursor, Hermes and Grok Bot document NONE; (4) live-verify install-from-source on VS Code (Copilot CLI, Codex and Hermes are verified as of v0.5.0 — see `docs/host-verification-results.csv`). The AUTHORITATIVE client roster (agent-plugins.org/compatible-clients, re-verified 2026-09-02) is NINE: VS Code, GitHub Copilot, Cursor, ChatGPT & Codex, Kiro, OpenClaw, Hermes Agent, Grok Bot (xAI), NanoClaw (Nanoco) — note VS Code is listed separately from GitHub Copilot, and Copilot in JetBrains is not listed at all. Claude Code, Antigravity, Gemini CLI and Qwen Code are NOT Agent Plugins clients — keep their native formats. Watch the two name collisions: xAI's "Grok Bot" is not the community `grok-cli` adapter, and Nanoco's "NanoClaw" is not NVIDIA's NemoClaw.

- Upstream host churn found by a full `gh api repos/<owner>/<name>` sweep of every `hostSource` repo (2026-09-02). Repo POINTERS in `site/src/platform-data.ts` are already corrected; what still needs live verification is whether the CONFIG CONTRACT moved with the product:
  - **Codebuff → Freebuff** (`CodebuffAI/codebuff` → `CodebuffAI/freebuff`, freebuff.com). Product renamed, so the display name, CLI binary and config dir may all have changed — verify before trusting the `codebuff` adapter's paths.
  - **Mux → Xum** (`coder/mux` → `coder/xum`, xum.coder.com). Same risk for the `mux` adapter's `mcp.jsonc` location.
  - **goose** moved out of the Block org (`block/goose` → `aaif-goose/goose`); product name unchanged, low risk.
  - **Open Interpreter** handle normalized (`OpenInterpreter/open-interpreter` → `openinterpreter/openinterpreter`); low risk.
  - **OpenCode** (`sst/opencode` → `anomalyco/opencode`, SST rebranded to Anomaly) and **OpenHands** (`All-Hands-AI/OpenHands` → `OpenHands/OpenHands`); product names unchanged.
  - **Roo Code is ARCHIVED** upstream (last push 2026-05-15) and is now marked `EOL` on the coverage wall via `hostLifecycle`. Decide whether to retire the adapter or keep it as a legacy target.

- `docs/ARCHITECTURE.md` carried a hand-written "29/35 hosts read the open AGENTS.md standard" tally whose denominator matched neither the registry (43 adapters) nor the memory-supporting set (39). It has been de-numbered rather than guessed at, because 33 memory adapters override `memoryTargets()` for probing reasons while still landing on AGENTS.md for project scope, so a grep cannot separate a genuine exception from a probe. If the exact tally is wanted back, derive it from the resolved project-scope target per adapter and pin it with a drift test — an un-guarded number here will just rot again.

## Completed Direction Work

- Added `audit` as a pre-install package identity lint for branded MCP packages. It checks package name/version/bin, `@ken-jo/agent-connector` runtime dependency placement, connector id/version drift, and `files` coverage before users run install.
- Moved first-touch developer onboarding into a root-level `/docs/guides` track and the scaffold wizard: `/docs/guides/mcp-beginner` starts with neutral MCP concepts, includes an architecture diagram plus ASCII flows for host/server/tool/hook behavior, and mentions agent-connector only after a one-host server works. The Guides track now has expandable beginner pages for how agent-connector fits, host hooks by CLI paradigm, HUD/statusline behavior, actions, and commands/skills/subagents/memory.
- Expanded `install <source>` intake beyond GitHub/raw git to include explicit `npm:<package>[@version]` packages and tarball archives (`archive:<path-or-url>` or direct `.tgz` / `.tar.gz`), with the same stable cache and connector package gate.
- Added a generated adapter capability snapshot for the public site. Coverage and platform docs can now summarize what agent-connector actually wires from the adapter registry, while human-authored host-native provenance remains separate in `platform-data.ts`.
- Added a generated host verification snapshot from `docs/host-verification-results.csv` and surfaced a public verification ladder on `/coverage`, separating full E2E, live-accept/auth-blocked, and install+doctor placement confidence.
- Added a generated release hygiene snapshot for `/coverage`, showing repository package version, npm latest, GitHub release snapshot availability, and CI/deploy workflow presence so release drift is visible instead of hidden in README badges only.
- 2026-07-02 top-host live sweep: do not inflate `VERIFIED_E2E`. Keep MCP tool-call E2E separate from `LIVE_RUNTIME_VERIFIED` (headless runtime/hook/MCP-load only), `MCP_INSTALL_VERIFIED_AUTH_BLOCKED` (offline accept/list but auth-gated turn), `INSTALL_DOCTOR_VERIFIED_NO_HEADLESS_MODE`/`live-placement` (real CLI placement + uninstall clean, no offline accept/runtime), and pure install/doctor results. The live verifier now generates its default `.acverify/agent-connector.config.mjs`, retains npm verification tools in `.verify-tools/package.json`, isolates Windows `LOCALAPPDATA`, and can download pinned Windows assets for cursor/droid/goose/omp/mistral-vibe.
- 2026-07-02 additional host sweep: `kilo-cli` has an official `@kilocode/cli` package and now reaches `LIVE_RUNTIME_VERIFIED` (`kilo mcp list` accepted config; `kilo run` fired events.log). `pi` has official `@earendil-works/pi-coding-agent` and now reaches live-placement with a content fixture (prompts/skills/actions/memory placement plus uninstall clean). Do not promote `mux` from install/doctor based only on the huge Electron desktop/installer asset; it lacks a headless CLI accept path. Do not promote `openhands` yet: `openhands-ai` 0.37.0 is official but its wheel console script points at a missing `openhands.core.cli` module in the downloaded artifact. Do not promote `hermes` yet: npm is an unofficial bridge, while latest official Python wheels require Python <3.14 and need a real Python-tool install/accept lane before scoring.
- 2026-07-04 coverage gap pass: added an honest `ADAPTER_PLACEMENT_VERIFIED` / `adapter-placement` tier for hostless IDE, GUI, and service adapters. `verify-host` now drives supported adapters through built install/uninstall in isolated HOME or project scope without requiring or pretending a real host CLI accept/runtime. Verified adapter placement for `codebuddy`, `kilo`, `vscode-copilot`, `jetbrains-copilot` (preferred project scope), `antigravity`, `kiro`, `roo-code`, `windsurf`, `zed`, `warp`, `trae`, `devin`, `hermes`, `nemoclaw`, `mux`, and `openhands`.
- 2026-07-04 safety-filter pass: keep `llms.txt` as a compact descriptive route map only. Detailed host ids, hook-event lists, SDK helper enumerations, marketplace driver lists, and instruction-like examples belong in `llms-full.txt` or the skill references. This reduces permission-review false positives when external web openers inspect the public site or agent-readable docs.
- Added blog discovery plumbing: prerender now emits `/feed.xml`, the site head advertises it as an RSS alternate, and the blog index links to the feed directly. The public blog currently carries one BUILDING test post with a cover image so image rendering can be reviewed before real articles are published.
- 2026-09-02 Agent Plugins as the SINGLE SOURCE OF TRUTH: `agent-plugin` (Agent Plugins 1.0.0, agent-plugins.org) is now the DEFAULT `package` format and the one bundle every spec-speaking host installs. What changed: (1) the emitter ships a portable launcher `bin/agent-connector.mjs` (home binary → PATH → `npx @ken-jo/agent-connector@^<version>`; stdio inherited, signals + exit codes forwarded) so mcp.json runs `node ${PLUGIN_ROOT}/bin/agent-connector.mjs serve …` with NO absolute path and no global-install prerequisite; (2) client extension namespaces are data-driven (`CLIENT_NAMESPACES`): `com.github.copilot/` (hooks via the `${PLUGIN_ROOT}` launcher, `agents/<n>.agent.md`, `commands/<n>.md`; read by Copilot CLI + VS Code + JetBrains) and `com.openai/` (hooks only, in the home-bin command form because Codex documents no root token for hook commands; wired through `plugin.json` `extensions["com.openai"].hooks` — Codex parses that object as its manifest overlay, per codex-rs/core-plugins/agent_plugin_manifest.rs, and prefers a root AP plugin.json over `.codex-plugin/`); (3) `hostHint` (EmitContext/PackageOptions) stamps `--host` for telemetry when a driver stages for a known host and restamps only the namespace that host reads; (4) local catalogs at `<out>/.claude-plugin/marketplace.json` + `<out>/.agents/plugins/marketplace.json` so the two-step copilot/codex marketplace install works from `package` output; (5) the codex + copilot-cli marketplace drivers stage THIS bundle (marker = root plugin.json with the AP `$schema`, `readAgentPluginManifest`), and MARKETPLACE_FORMAT_BY_PLATFORM routes codex, copilot-cli, vscode-copilot, jetbrains-copilot, kiro, hermes → agent-plugin (cursor keeps cursor-plugin and openclaw claude-plugin: their own docs say the client-specific format is the one that carries hooks/commands); (6) `codex-plugin` / `copilot-plugin` are RETIRED — `LEGACY_FORMAT_ALIASES` + `resolvePackageFormat` keep the names parsing with a deprecation line. Live-verified macOS 2026-09-02: codex-cli 0.149.0 (marketplace add + plugin add + list installed/enabled), GitHub Copilot CLI 1.0.80 (marketplace add + install + list), hermes `plugins doctor` OK, launcher resolution order + exit-code passthrough. Docs updated in README, llms-full.txt, ARCHITECTURE.md, site docs-data/PackagingGuide, host-verification-results.csv. Open: Cursor has no documented AP client namespace (dual `.cursor-plugin/` format instead); Kiro documents `dev.kiro/` (steering) but no hook layout; OpenClaw reads only `extensions["ai.openclaw"]` (activation) — add namespaces to CLIENT_NAMESPACES as they get documented.
- Hardened the statusline/action SDK around currently supportable host behavior. Statusline now has shared/per-host options (`refreshInterval`, `respectUserColors`, `hideContextIndicator`, framework-enforced `maxLines`) and adapters write only supported command-driven settings for `antigravity-cli`, `claude-code`, and `qwen-code`. Actions now carry UI metadata (`label`, `icon`, `placement`, `confirm`) plus per-host overrides and capability metadata describing whether the host affordance is exec, exec-file, manual-hook, paste, plugin-command, or task. Verified with `npm run typecheck`, `npm run build`, and the focused statusline/action/docs drift regression suite.

## Public Coverage Rule

Public-facing support lists should highlight:

- closed-source flagship hosts, because stars do not apply;
- open-source hosts with at least 1,000 GitHub stars;
- all lower-star or early-stage adapters only in developer/reference contexts.

This keeps the first impression focused and avoids listing immature or low-signal support targets as if they were equally important launch platforms.
