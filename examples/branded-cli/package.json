{
  "name": "@acme/acme-db-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Acme's database MCP server + hooks, shipped with its own branded CLI on top of agent-connector.",
  "type": "module",
  "mcpName": "io.github.acme/acme-db",
  "bin": {
    "acme-db": "./bin.mjs"
  },
  "files": [
    "bin.mjs",
    "agent-connector.config.mjs",
    "acme-db-mcp-server.mjs"
  ],
  "dependencies": {
    "@ken-jo/agent-connector": "../.."
  }
}
